import React from 'react';
import { PropertyForm } from '@/components/properties/property-form';

const PropertiesNew = () => {
  return <PropertyForm isEditing={false} />;
};

export default PropertiesNew;
