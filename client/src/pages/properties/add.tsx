import PropertyForm from '@/components/properties/PropertyForm';

const PropertyAdd = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight mb-6">Add New Property</h1>
      <PropertyForm />
    </div>
  );
};

export default PropertyAdd;
